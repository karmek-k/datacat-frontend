import { createTheme } from '@material-ui/core/styles';

export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: '#4685CF'
    },
    secondary: {
      main: '#D77B7B'
    }
  }
});
